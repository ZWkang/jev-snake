## Purpose

让无需登录的游客以只读方式观察服务端对局、玩家身份和真实事件，并进入历史与回放。首版不开放人类操控、创建对局或接管入口，人机共玩留待后续独立设计与实现。

## ADDED Requirements

### Requirement: 独立实时观战入口

系统 SHALL 提供 /watch/:matchId 对局观战页面，进入即读取该局最新已确认状态并建立 WebSocket 订阅。历史列表中 ready/running 对局进入观战，终局进入回放；回放模式 SHALL 明确标识不会自动跟随新进度，并提供返回实时观战的入口。

#### Scenario: 从列表进入进行中对局
- **WHEN** 游客点击进行中或等待开始对局的观战链接
- **THEN** 进入绑定该 matchId 的实时页面，后续局面自动更新，不进入暂停的回放页

#### Scenario: 刷新或切换观战对局
- **WHEN** 游客刷新观战地址或选择另一场对局
- **THEN** URL、选中项与订阅目标一致，旧订阅清理，旧连接的延迟回调不覆盖新局面

### Requirement: 游客免登录且只能查看

系统 SHALL 允许游客无需登录读取允许公开的对局列表、当前状态、事件和回放，不以注册拦截查看流程。游客没有创建对局、转向、原局暂停、重开、调速或接管的权限，界面也不得提供可用的相关操作入口。只隐藏按钮不足以满足只读要求，服务端写接口必须验证控制凭证。

#### Scenario: 初次访问
- **WHEN** 新游客首次打开网站
- **THEN** 能直接查看公开对局或真实空状态，不需要注册或登录，不出现自己玩或接管入口

#### Scenario: 游客提交游戏动作
- **WHEN** 游客按方向键、Space 或 R，或者不带合法控制凭证调用写接口
- **THEN** 浏览器不提交游戏动作；直接写请求被拒绝，原局不变

### Requirement: 可解释的局面与事件

观测界面 SHALL 展示局标识、最新已确认局面、分数、步数、蛇长、当前方向、玩家身份和真实事件。Agent 决策说明仅在确实由其提供且允许公开时呈现；没有说明时只显示动作和结果，不编造推理。

#### Scenario: 没有决策说明
- **WHEN** 记录只有方向和结果
- **THEN** 展示动作与结果，不补写 Agent 的内部推理

#### Scenario: 当前没有可观测对局
- **WHEN** 当前没有进行中的公开对局
- **THEN** 显示无对局状态和历史入口，不自动创建假对局或引导游客开局

### Requirement: 更新状态与观战隔离

系统 SHALL 区分正在同步、更新中断与原局结束。更新断开时保留最后确认位置并提示，不自行模拟蛇继续移动；恢复后根据服务端序列读取缺失事件或重新定位已确认状态，不能重复应用事件。游客断线、切换标签或查看历史不得改变原局。

#### Scenario: 观测更新断开
- **WHEN** 观测页面失去服务端更新
- **THEN** 显示最后确认局面与中断状态，不声称画面仍实时，也不替玩家继续操作

#### Scenario: 返回观战
- **WHEN** 游客从回放返回当前对局
- **THEN** 读取服务端最新已确认位置，不把回放游标写回原局
