## 1. Hono 服务与公开契约

**定稿范围：** 游客只读、Hono + SQLite + WebSocket、固定节拍持续移动、开局可配置速度。勾选项表示已完成实现与相应验收；未勾选项仍待完成，证据见 validation.md。

- [x] 1.1 新建独立 `server/` 与 `shared/snake/`，接入 Hono Node 适配器、ws、better-sqlite3 及对应类型；锁定依赖并验证安装、独立类型检查与启动入口可运行。
- [x] 1.2 添加后端开发、构建、启动和测试脚本及独立 TS 配置；验证前端原构建仍可执行，后端构建产物能用 Node 启动。
- [x] 1.3 定义公开状态、摘要、版本化事件、WS 控制/观测消息与错误契约；用校验测试确认未知消息、非方向值和不兼容版本明确拒绝。

## 2. SQLite 与原子记录

- [x] 2.1 建立显式 SQLITE_PATH、版本化迁移和 matches/match_events/control_requests 表；用真实临时文件验证 schema、唯一约束、外键、WAL 配置、SQLite 版本与重启读取。
- [x] 2.2 实现最新状态、事件、摘要和请求确认的原子事务；注入真实写入失败验证全部回滚，提交前没有成功响应或局面广播。
- [x] 2.3 实现请求标识去重与内容冲突校验；验证相同请求重复提交只应用一次，不同内容复用同标识明确冲突。
- [x] 2.4 实现公开查询与游标分页，覆盖参与者/结果筛选；验证实际数据库空状态、页间不重复和公开响应不包含控制 token/hash。
- [x] 2.5 实现迁移与持久化错误暴露；验证数据库不可写或迁移失败时明确报错，不自动切换空库/内存库或生成示例记录。

## 3. 服务端规则与控制权限

- [x] 3.1 实现可配置尺寸（默认 24×18）的纯规则引擎、初始蛇、合法方向与碰撞/满盘判定；测试直接反向拒绝、腾出的尾格、三类死亡、增长和完成状态。
- [x] 3.2 实现可配置数量（默认 12 个）的随机障碍和奖励位置生成；固定随机输入测试数量、唯一性、出生安全区、连通性、无重叠和有限候选扫描。
- [x] 3.3 实现苹果 +10/增长与星星 +30/不增长；用服务端有效时间测试生成、8 秒过期、到期与移动同时发生时先过期、满盘及苹果优先占用唯一奖励格。
- [x] 3.4 实现受保护的创建请求与每局控制凭证校验；测试游客不能创建、不能跨局控制，重试不重复创建且所有公开结果和日志不泄露密钥。
- [x] 3.5 实现控制者主动结束并创建独立下一局；验证原局以真实原因保留，新局使用新标识，游客页面不能调用这一流程。
- [x] 3.6 实现 ready/start、开局 tickIntervalMs 配置与单调时钟固定节拍调度；用可控时间验证 125ms 下 500ms 四步、500ms 下每秒两步、无新动作继续原方向、回调晚到按计划顺序补齐、终局停止和 schedulerLagMs 记录。
- [x] 3.7 实现计划 tick 方向槽、observedSeq/expectedStateHash 校验及精确截止规则；验证 100ms 接纳目标 125ms 的动作，125ms/150ms 拒绝为迟到，未来目标接纳、接纳及执行时预期失效明确拒绝/取消，同 tick 不同动作冲突，成功请求晚到重发仍去重。
- [x] 3.8 持久记录方向 accepted/rejected/applied/cancelled、观察位置和收件时间；验证 accepted 不提前移动，端到端反应耗时与客户端报告 inferenceMs 分开标注，拒绝动作不进入轨迹。

## 4. WebSocket 观战与控制

- [x] 4.1 建立独立 watch/control 升级入口与消息处理；使用真实 WS 客户端验证匿名观战可连接、未授权控制握手失败、观测连接的 action/stop 不进入引擎。
- [x] 4.2 实现 start/action/stop 确认与持久化后的广播；验证接纳时只存 pending，目标 tick 事务后再记录 applied，重复 start 不重置时间锚点，响应丢失重发不重复应用。
- [x] 4.3 实现 afterSeq 续读与连接建立期间的无缺口订阅；并发提交和重连测试确认顺序完整、重复可识别、无中间漏帧。
- [x] 4.4 测试提交后广播前进程停止，再启动后通过持久事件恢复；确认所有已确认事件仍可读取。
- [x] 4.5 清理断开的订阅及网络等待，避免阻塞事务和引擎；验证游客全部离开不改变对局，慢观众不会改变规则或丢失持久事件。

## 5. 游客观战、历史与回放

- [x] 5.1 实现首页观战、历史、指定局回放路由与前后端 HTTP/WS 连接配置；真实后端验收游客免登录、无自己玩/接管/原局调速入口。
- [x] 5.2 接入统一格坐标 SVG、多色蛇身、障碍和生成奖励；验证物件中心、四向相邻和不同窗口尺寸缩放一致，资源加载失败可见。
- [x] 5.3 接入 WS 顺序状态、分数和事件，显示同步/断线/补齐状态；测试重复 seq、不连续 seq 和标签切换不提交游戏控制。
- [x] 5.4 基于连续已确认状态实现沿正交路径的移动插值及奖励反馈；检查转弯、增长、跳转和减少动态效果，不显示未经确认的下一步。
- [x] 5.5 实现历史排序、参与者/结果筛选、原局速度、空状态及完整性提示；从真实持久记录验收不同速度局、完整局和中断局在刷新后仍可访问。
- [x] 5.6 实现按快照定位的回放、逐步前后、时间轴、0.5×/1×/2×/4× 与事件跳转；验证不调用随机源/Agent、不更改原局，迟到/取消事件可见但不显示为实际转向。
- [x] 5.7 完成可见焦点和响应式布局；检查桌面及 375px 窗口，保证触控查看可用、无页面横向溢出。

## 6. 集成交付

- [x] 6.1 完成活动局重启与控制断线集成测试；验证控制断线仍持续移动，进程重启将原 running 局标为 server_restart 中断且取消 pending，ready 不自动启动，不补跑停机时段。
- [x] 6.2 运行规则测试、SQLite 持久化测试、真实 WS 测试、前后端类型检查和构建；保留真实结果并修复新增失败。
- [x] 6.3 对改动文件运行 Biome；如旧配置不兼容则确认根因后最小修复并重新验证，不静默跳过。
- [x] 6.4 使用真实程序化控制客户端按不同 tickIntervalMs 完成对局，包含故意迟到的请求，多个游客同步查看后回放；验证速度如实生效、迟到结果可查、直播/历史/回放相同 seq 的局面一致。
- [x] 6.5 更新 README 与环境变量示例，记录服务启动、持久目录、HTTP/WS 接口和部署边界；按文档从新数据目录启动并完成验收。

## 7. JEV 与素材

- [x] 7.1 实现独立 JEV runner 和受保护的局面预判接口；测试真实 Decisions API 请求形状、概率校验、单个在途请求、迟到及预期失效，不使用聊天或模拟模型结果。
- [x] 7.2 接入环境变量配置、缺少密钥状态与调用失败记录；用传输层测试验证错误显式可见，真实密钥可用时完成模型联调并注明证据。
- [x] 7.3 接入用户提供的片头/获胜视频与生成奖励素材；浏览器验证资源可加载、默认静音、可跳过/暂停和减少动态效果，视频不遮挡实时棋盘。

## 8. 决策输入查看与并发验收

- [x] 8.1 保存真实发送给 JEV 的请求正文，随决策结果持久化；验证正文与实际 HTTP body 一致，不包含 Authorization 或密钥，兼容旧记录。
- [x] 8.2 回放增加决策输入面板，显示观察 seq、预判位置、目标 tick、原始 JSON 与复制；随时间轴更新，旧记录明确显示未保存正文，验证桌面与手机布局。
- [x] 8.3 观战目录读取全部活动局并保留当前选择；记录 30 场、300ms/步的真实并发结果，完成针对性测试与浏览器验收。

## 9. 当前步决策、导航与直连接入

- [x] 9.1 以当前实际局面请求 JEV，只允许控制紧接着的一次移动；服务端拒绝未来队列与旧观察改绑，runner 每个实际 tick 最多询问一次，移除预判参数，保留旧回放语义并验证快慢响应。
- [x] 9.2 顶部导航移入统一布局，固定观战/历史入口，回放归属历史；使用路由 active 状态与一致样式，验证桌面、手机及浏览器前后导航。
- [x] 9.3 接入 Typesafe 官方直连、独立密钥与实际模型版本，明确记录 provider，不自动回退 OpenRouter；完成真实 500ms 联调与延迟对照，更新文档。
- [x] 9.4 区分回放当前尚无响应与整局没有响应，展示真实决策数量、首次响应时间并支持跳转，验证延迟首响应对局。
- [x] 9.5 接入已探针验证的四方向事实 context，保留四个模型选项和真实请求；验证事实与规则一致，并运行一场 500ms 真实对局。
- [x] 9.6 独立探测无模型响应的连接与返回阶段，补充请求开始/取消/失败日志，修复终局竞态吞掉真实错误并完成回归验证。
- [x] 9.7 概率合计不为 1 仅作诊断，不阻断有效方向；保留原始概率，验证 0.99/1.01 与真实 runner 持续执行，移除不必要的中断条件。
- [x] 9.8 区分实时观战与回放入口，添加按 matchId 订阅的独立观战页；验证历史入口、WebSocket 自动推进、刷新、从回放返回和导航激活状态。

实施状态：47/47 项完成，实时观战与回放入口已区分并完成真实服务端与浏览器验收。概率总和不再决定是否执行方向。外部模型调用仍存在秒级建连和响应等待，未声称已消除。完整证据见 validation.md。
