## MODIFIED Requirements

### Requirement: 可解释的局面与事件

观测界面 SHALL 展示局标识、最新已确认局面、分数、步数、蛇长、当前方向、玩家身份和真实事件，以及该局推进模式。fixed 展示配置速度；response 展示“随模型响应”以及有明确定义的实测步间隔/平均步频，不用固定间隔倒数伪造速度。Agent 决策说明仅在确实由其提供且允许公开时呈现；没有说明时只显示动作和结果，不编造推理。

#### Scenario: 没有决策说明
- **WHEN** 记录只有方向和结果
- **THEN** 展示动作与结果，不补写 Agent 的内部推理

#### Scenario: 当前没有可观测对局
- **WHEN** 当前没有进行中的公开对局
- **THEN** 显示无对局状态和历史入口，不自动创建假对局或引导游客开局

#### Scenario: 等待响应时观战
- **WHEN** response 对局正在等待下一条有效动作
- **THEN** 棋盘保持最新确认位置，清晰展示“等待下一次决策后移动”；不能把正常等待显示为观战连接断线，也不能在没有发送证据时声称模型正在推理

#### Scenario: 快速或不均匀响应
- **WHEN** 有效动作以不均匀间隔连续到达
- **THEN** 按已提交状态更新分数、轨迹和事件；展示层不要求每个固定时长动画播完再确认下一局面，不影响服务端步频

### Requirement: 更新状态与观战隔离

系统 SHALL 区分正在同步、更新中断、响应模式等待动作与原局结束。更新断开时保留最后确认位置并提示，不自行模拟蛇继续移动；恢复后根据服务端序列读取缺失事件或重新定位已确认状态，不能重复应用事件。游客断线、切换标签或查看历史不得改变原局。

#### Scenario: 观测更新断开
- **WHEN** 观测页面失去服务端更新
- **THEN** 显示最后确认局面与中断状态，不声称画面仍实时，也不替玩家继续操作

#### Scenario: 返回观战
- **WHEN** 游客从回放返回当前对局
- **THEN** 读取服务端最新已确认位置，不把回放游标写回原局

#### Scenario: 等待期间显示时间
- **WHEN** response 模式在已建立观战连接下等待数秒
- **THEN** 运行时长和星星倒计时可依据服务端时间基准继续显示，位置和分数只随已提交事件变化；断线时不继续声称仍掌握实时局面
