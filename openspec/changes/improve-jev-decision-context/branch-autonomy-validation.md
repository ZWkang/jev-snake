# 分叉死亡证据与模型自主决策验收

日期：2026-09-19。按用户最新要求，程序只固定游戏规则并提供可验证事实，吃食物、探索、保留空间等策略由 JEV 自主决定。

## 改动

- 删除提示词中危险→空间→食物的固定排序、未尝试方向优先、按无苹果返回次数/执行次数选择等策略指令。保留游戏目标、实际计时/两步协议、证据含义及真实历史。
- 四方向和十六方向对均完整发送，模型返回方向原样保存与提交；游戏规则仍判定非法反向/碰撞。没有程序代选、静默兜底或自动循环终止。
- 新增有限证据组合器：沿唯一合法通道到首个分叉，只在所有合法出口都有已有死亡证书时给入口标记 `proven_fatal`。存在无法证明的分支、循环或获胜可能即保持未证明。
- 组合器不穷尽任意多层分叉，不新增深度/时间/状态数量上限，也不把未知当作安全。原始 forcedPath 的 branch/steps 含义保持不变。

## 第644步的真实回归

来源 `6cf14499-8182-4831-99cf-11dc41ebaee0`，观察 seq1297 / tick644，蛇头 `(10,5)`。实际在下一步向右进入 `(11,5)` 后，up 最迟6次移动尝试碰撞，down 最迟4次；新证书从入口之后起算，最大上界6。向左未被判死，已有26步真实合法路线验证到 tick670 `(4,1)`。

旧静态区域判断只知道右侧共9格，边界最早第9步腾出，因此没有证明；组合器利用“上下每个分支都已经有死亡证书”补足该缺口。

## 模型实测

沿用当前 OpenRouter / typesafe/jev-1.13，预先安排1次保存原文对照和3次新输入请求，全部正常返回。

| 输入 | 实际选择 | 向左概率 |
| --- | --- | ---: |
| recorded-input | left | 64% |
| facts-and-model-strategy-1 | left | 95% |
| facts-and-model-strategy-2 | left | 93% |
| facts-and-model-strategy-3 | left | 94% |

本次重新提交旧正文也选择了左（64%），而原对局记录是右（51%）；这是模型输出变化，不能宣称旧输入必然向右。新输入只给事实与目标，3次均自主向左（93%–95%），不代表所有地图都能通关。此前7×5通关记录属于当时带策略偏好的提示词，未重写历史，也不作为当前版本的整局成绩。

## 验证

- 最终全量18个文件 / 208项测试通过；类型检查、oxfmt/oxlint和生产构建通过。
- 覆盖真实分叉、唯一通道前缀、单可活出口、循环、尾巴释放、增长未知、胜利优先、已知第二步、状态与RNG不变。
- 另加传输测试：即使模型选择已标为必死的方向，也原样返回，证明没有替换模型决策。
- 类型、格式/lint与生产构建检查运行结果见 `.data/qa/branch-proof/`。
- 本机该局面100次请求构建：p50 0.235ms，p95 0.409ms；只作局部成本观察，不承诺所有地图的上界。

## 证据路径

- `tests/fixtures/edge-branch-trap.json`，来自已保存真实局面，含已验证的向左路线。
- `.data/qa/branch-proof/probe.json` / `probe.log`：完整实际输入与输出，不含密钥。
- `.data/qa/branch-proof/{tests-final,typecheck-final,build-final}.log`。
- `.data/qa/branch-proof/local-cost.json`。

本轮未启动新对局，未修改既有对局或恢复已结束控制进程。
