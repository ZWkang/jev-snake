# 实施与验收记录

验收日期：2026-09-19。当前完成 47/47 项，包含当前实际局面决策、统一导航与 Typesafe 官方直连。早期预判模式的实验记录保留在下文，不代表当前运行逻辑。

## 程序验证

- `npm run test:snake`：5 个文件、36 个测试通过，覆盖规则、SQLite 事务与重启、真实 HTTP/WS、两个 provider 的传输契约与凭证隔离、runner 快慢响应、当前步绑定、请求正文持久化、活动目录分页和回放记录完整性。
- `npm run typecheck`：前后端类型检查通过。
- `npm run build`：后端与前端完整构建通过；本地日志为 `.data/qa/build-final.log`。
- 改动文件的 Biome 检查通过；`git diff --check` 通过。
- 开发服务运行于前端 3000、Hono 3001；经前端代理访问 `/api/health` 返回 `status: ok`，HTTP 与 WebSocket 均通过真实客户端验证。
- 修复 Nitro 在 Vite HTTP 代理前截获 `/api` 的问题：开发 HTTP 使用 Nitro devProxy，WebSocket 使用 Vite 升级代理。
- 数据库尾部事件缺失曾造成空页仍报告有后续记录；补充失败用例后修复为明确的记录损坏错误，避免回放无限拉取。

## 真实程序化对局

以下记录由真实服务端规则与 HTTP/WS 控制产生，保存于本地 SQLite；参与者明确标注“非 JEV”，没有模型推理结果。

| 对局 | 参数与结果 | 验证内容 |
| --- | --- | --- |
| `941db546-34dd-4d7f-be10-e368b9656e13`，协议验收 · 非 JEV | 600ms/步，0 个障碍，17 步，10 分，撞墙 | 持续移动、吃苹果增长、实际分数、历史与回放一致 |
| `b67fdb06-9053-413b-b898-e9c88986d8ce`，转向验收 · 非 JEV | 250ms/步，0 个障碍，12 步，0 分，撞墙 | 未来 tick 3 的向上动作接纳后按计划实际应用 |

自动化规则与集成测试另覆盖随机障碍、迟到拒绝、预期局面失效取消、重复请求、连接续读与重启中断。上述两局采用无障碍参数以明确观察协议与坐标行为。

## 浏览器交互与视觉

先前已在 ego lite 空间 6 完成浏览器验收。用户随后要求切回 agent-browser，项目 `AGENTS.md` 已相应修改，后续验收使用独立 `snake-qa` session。

- 首页、历史、指定局回放均读取真实后端记录；游客没有开始、控制或原局调速入口。
- 回放从初始 000 分，到第 2 步吃苹果变为 010 分；逐步、关键事件定位、4× 播放至终局和回退均已验证。回放倍速不修改原始速度。
- 修复奖励位置未随状态更新的问题：Solid 的奖励子组件使用响应式坐标。第 2 步的新苹果位于 `(19,13)`，SVG 变换为 `translate(608 416)`；回到初始状态恢复为 `translate(288 288)`。格子使用统一的 32 单位坐标。
- 桌面布局与 375px 视口已检查；375px 下 app/body 宽度均为 375px，棋盘宽 337px。历史表格在自身容器内横向滚动。
- 历史参与者筛选、结果空状态、Tab 可见焦点和减少动态效果已检查；验收时未捕获页面 JS 错误或未处理拒绝。
- agent-browser 已重新打开首页、进入历史与回放，并通过真实点击跳到吃苹果事件：显示第 2 步、蛇长 5、010 分，新苹果位置正确。
- agent-browser 使用明确的 CSS 选择器点击播放：2 秒后读到 seq 4、第 3 步、010 分，随后到达 seq 18 并自动暂停；点击从头开始后回到 seq 0、000 分。375px 下再次确认 app/body 宽度为 375px，截图为 `.data/qa/agent-browser-replay-mobile.png`。
- 本地截图：`.data/qa/replay-desktop-fixed.png` 为先前修复后的验收截图；`.data/qa/agent-browser-replay-desktop.png` 为切换 agent-browser 后重新生成并完成视觉检查的截图。
- 本机 agent-browser 版本为 0.4.4；部分 `select` / `get value @ref` 命令返回 CLI 参数或选择器错误。这些是工具执行失败，未计为页面功能通过，也未修改应用代码以规避它们。

## 素材

- 用户桌面两份视频已复制到 `public/assets/snake/`，并生成海报。
- 片头实际时长 6.08 秒、1280×720；浏览器原生播放验证为播放中且静音。
- 名称为“3s贪吃蛇”的视频实际时长 4.064 秒、960×960；已验证资源 HTTP 200 与浏览器元数据。
- 生成的奖励透明素材已接入统一网格；视频不覆盖实际进行中的棋盘。

## 真实 JEV 联调

用户将 `OPENROUTER_API_KEY` 配置于本地 .env 后，重启开发服务读取配置，健康接口返回 `jevConfigured: true`。调用真实 OpenRouter Decisions API，请求模型为 `typesafe/jev-1.13`，两场响应实际版本均为 `typesafe/jev-1.13-20260917`。密钥未输出到日志或写入验收文件。

| 对局 | 速度 / 预判提前量 | 结果 | 动作确认 | HTTP 往返耗时 |
| --- | --- | --- | --- | --- |
| `8ffdcd51-bc4e-4712-add2-f76b85a8ed51` | 500ms/步，1000ms 预判 | 23 步，10 分，撞墙 | 19 次接纳、19 次按原目标 tick 生效 | 276–1287ms，中位数 330ms |
| `e172424d-b053-4053-a10b-26e752d16661` | 125ms/步，500ms 预判 | 25 步，0 分，撞墙 | 5 次接纳并生效，1 次 late_action 拒绝 | 291–982ms，中位数 345ms |

两场均采用 24×18、12 个随机障碍和随机保存的种子。统计覆盖已返回并提交的模型决策，requestMs 是 HTTP 往返时间，不是纯推理耗时。首个 125ms 对局请求耗时 982.373ms，目标 tick 5 已过；检查完整事件确认该 requestId 未出现在任何实际应用动作中，没有改目标或补做过期动作。

- 两局分别保存 44、33 条连续事件；逐条核对 seq、终局分数与蛇身，全部一致。
- 对所有接纳动作核对实际应用事件：requestId 对应且 applied tick 等于原 targetTick。两局最大 schedulerLagMs 分别约 1.25ms、1.35ms。
- agent-browser 验证真实直播中的方向概率与状态、历史列表中的两场 JEV 记录，以及 125ms 回放第 7 步的迟到事件：显示目标第 5 步、982ms、“动作抵达时已过期”。
- 500ms 对局以 4× 回放到 seq 43，显示第 23 步、010 分、蛇长 5 和撞墙终局；回到开头恢复 seq 0、000 分、蛇长 4。浏览器未记录页面错误。
- 完整公开快照与事件保存在本地 `.data/qa/jev-<matchId>.json`；汇总为 `.data/qa/jev-live-validation.json`，迟到事件截图为 `.data/qa/jev-late-replay.png`。

## 本轮联调修复

- `--lookahead-ms` 原本在创建对局后校验：负数留下 ready 局，小数进入运行后被服务端拒绝。回归测试先复现不应出现的记录，再将非负整数校验前移至创建前，与现有服务端参数契约一致。
- 新增真实 runner 子进程测试：缺少密钥时不创建对局；上游 HTTP 503 时退出码非零、输出明确错误，并经真实 WS 将 SQLite 对局持久标记为 interrupted/model_error。仅测试中的上游传输被替换为失败响应，没有模型成功结果，也不使用用户凭证。
- OpenRouter 实际返回带日期的版本名，原首页精确比较别名导致仍显示“已配置”；现在根据该局请求模型与已保存决策确认“JEV 已接入”，原始版本名继续保留。
- 观战目录每 5 秒刷新重建 option 时，原生下拉框选中项会丢失；为 option 绑定当前选中状态。刷新后实际读取仍为同一局，与棋盘一致。

实现与本地验收已完成。未归档 change，未创建提交，也未部署生产环境。

## 30 场并发与速度调整

用户将实验的最短间隔改为 300ms。最初的 125ms 批次停止时已无活动局；其中 2 个 runner 未在 SIGTERM 后退出，确认对应对局已结束后清理进程，原始日志保留，未将其记为正常退出。这个旧批次退出异常的根因尚未确定。

重新启动 30 场独立随机地图的 300ms 对局，根据实际 startedAt/endedAt 计算并发峰值为 30。该批次全部结束，服务保持健康：

- 28 场碰撞结束，2 场因 `JEV probabilities do not sum to one` 中断；没有重试伪装成功或自动归一化概率。旧错误未记录具体概率值，已补充后续错误的实际模型、概率和值，未声称已修复上游响应问题。
- 总分 290，最高 40 分；577 条已保存决策、537 次实际应用、36 次迟到拒绝。
- HTTP 往返中位数 332ms、P95 974ms、最大 1384ms；最大服务调度延迟约 38.09ms。统计仅覆盖返回并提交的决策。
- 报告与各 runner 日志在 `.data/qa/batch-30-300ms-2026-09-19T07-03-07-775Z/`。
- 修复观战目录只读最近 20 场导致漏掉活动局的问题：分别按游标读完 running/ready，和最近历史去重合并；当前已选对局移出最近页时仍保留对应选项。新增 30 个活动局跨页回归测试。

## 决策输入面板

新增可选 `decision.request` 保存请求正文快照，由同一份对象生成 OpenRouter HTTP body 并随返回决策进入真实 SQLite 事件。认证头不进入正文，契约拒绝额外 headers 字段；旧记录缺失该字段仍可读取。

- 回放新增决策选择器、观察 seq、执行目标、预判蛇头与模型名称、只读 JSON 和复制按钮。选择决策同步定位时间轴；回放位置改变时显示相应已记录决策。
- 300ms 真实验收局 `44f448e8-7e0d-40ea-8697-0be94ef20c21` 得到 10 分。浏览器对 seq 7 展示的 JSON 与 API 保存正文作逐项一致性比较，通过；显示观察 seq 5、目标 tick 6、预判蛇头 `(12,9)`。
- 复制按钮显示“已复制 JSON”，正文为只读；375px 下 app/body 均为 375px，面板 343px、正文 305px，无页面横向溢出。截图保存在 `.data/qa/decision-input-desktop.png` 和 `.data/qa/decision-input-mobile.png`。
- 旧局 `8ffdcd51-bc4e-4712-add2-f76b85a8ed51` 明确显示“这条旧记录未保存请求正文”，没有生成替代输入。
- 用户追加的两个 500ms 对局均已完成并保存请求：`7b5cc043-05df-49a1-9d65-35483ef67c50` 为 34 步、20 分；`12bf28df-5e73-47bf-9091-7124de9e587d` 为 18 步、0 分，均撞到障碍。

第二局最后一次决策在 tick 13（seq 30）接纳，计划 tick 18：请求里预判蛇头为 `(20,13)`，障碍包含 `(20,14)`，模型仍返回 down（50%，right 40%、left 8%、up 2%，confidence 0.35）。实际 tick 18 用同一 requestId 应用并撞到 `(20,14)`；左右邻格当时均无障碍和蛇身。该现象是已保存的模型选择；同时当前运行器会提前填充多个未来 tick，该次动作未在 tick 17 再次询问或替换。此次未擅自修改动作调度或添加自动避障。


## 当前步决策与统一导航（最终运行逻辑）

用户明确要求根据当前实际局面决定紧接着的移动。已移除 /projection、--lookahead-ms 和未来动作队列生成；新增 /decision-context。该接口不推进预测蛇身，返回当前已提交状态，targetTick 恒等于当前 tick + 1。服务端独立校验观察 tick、目标和实际局面摘要，拒绝未来目标及旧观察改绑。runner 接纳后等待真正移动，不因同一步内的新 seq 提前再次询问。

- 新增真实 runner 子进程测试：10ms 传输响应下，每个实际 tick 仅询问一次；650ms 响应配 300ms 步长时，旧结果明确 late_action 且不进入轨迹。测试仅替换上游传输，HTTP、WS 和 SQLite 均真实。
- 服务端测试验证未来目标拒绝、方向槽已占用时不会自动跳到后续 tick、旧 observedSeq 即使配新摘要也不能改绑，以及移动前奖励变化时取消失效动作。
- OpenRouter 的 5 场 500ms 复验全部满足实际输入与当前 tick 一致、目标仅为下一次移动；3 场碰撞结束，2 场因概率总和 0.99 中断。报告：.data/qa/current-step-5x500-2026-09-19T07-24-42-816Z/report.json。
- 顶部 Header 只在根布局挂载一次，固定两项导航；观战仅精确匹配 /，历史及其回放子路由激活历史。浏览器验证三个页面仅一项 active、一个 Header，375px 下 app/body 均为 375px。新决策面板显示“观察蛇头”和实际局面说明；旧预判记录保留相应标签。

## Typesafe 直连与延迟样本

依据 [官方 API](https://docs.typesafe.ai/api) 与 [模型文档](https://docs.typesafe.ai/models)，使用 POST https://api.typesafe.ai/v1/systemone，固定模型 jev-1.13.0。用户将独立 TYPESAFE_API_KEY 加入 .env 后，GET /v1/models 实际返回 HTTP 200。默认 JEV_PROVIDER=typesafe；OpenRouter 仅在显式选择时使用其独立密钥，没有自动回退。provider、请求正文和响应模型随决策保存。

首次直连采样因开发代码格式化触发服务重载而中断，没有记录到决策响应，不计入延迟样本；对应 server_shutdown 记录与日志保留于 .data/qa/typesafe-5x500-2026-09-19T07-34-19-789Z/。

在代码检查结束、服务保持稳定后重新并行执行 5 场 500ms 对局。采样目录：.data/qa/typesafe-stable-5x500-2026-09-19T07-35-27-155Z/。

| 指标 | Typesafe 直连 | 之前的 OpenRouter 当前步实验 |
| --- | --- | --- |
| 有效已记录响应数 | 18 | 69 |
| HTTP 往返中位数 | 319.916ms | 329.675ms |
| 最小值 | 267.528ms | 265.907ms |
| 最大值 | 3479.841ms | 1171.663ms |
| 迟到响应数 | 8 | 12 |

统计包含通过响应校验后提交的冷启动及迟到响应；不包含未返回决策的网络失败或不合法响应。地图、采样时间及样本数不同，不是严格同局 A/B，也不能把 HTTP 往返时间称为纯模型推理耗时。本次直连中位数与 OpenRouter 接近，不能据此承诺明显提速。

Typesafe 5 场结果为 3 场碰撞结束（0/0/10 分）、2 场中断。中断原因分别为 TLS 建立前 ECONNRESET，以及模型返回概率总和 0.99。官方 API 明确声明概率和为 1，因此继续保留校验和原始错误，没有自动归一化或替模型改选方向。

汇总报告为 .data/qa/provider-latency-comparison.json。浏览器已验证直连记录 24c1bb56-3c34-48df-ba31-34df1aacdc9a 的输入面板显示 jev-1.13.0、Typesafe 直连、实际观察蛇头与仅用于下一次移动的说明。


## 首次响应较晚的回放提示与 context 对照

对局 8a7994a2-eea7-48d7-b53d-1f1936ffdb12 共 55 条事件、20 条模型决策、16 次实际应用、4 次迟到。首个响应在游戏时间 7223.532ms（tick 14 / seq 16），HTTP 请求耗时 7214.598ms；首次有效转向在 tick 16。浏览器正常播放至 8.2 秒时已显示“向上”、269ms 和目标 tick 17，未复现全程无决策。

原回放 0 秒的“还没有收到模型决策”文案容易被理解为整局没有响应。已在该状态显示真实的整局决策数、生效数、首次响应时间和“跳到首次决策”按钮。浏览器初始状态显示 20 次 / 16 次 / 7.22 秒，点击后实际跳至 seq 16 并展示 7215ms 的迟到响应。类型检查、构建、Biome 和 diff 检查通过；没有修改历史结果或提前展示尚未发生的决策。

用户询问 context 是否需要优化后，对同一局最后一个实际输入做了两种静态 API 探针（没有控制游戏）：

- 蛇头 (8,6)，向左；左邻 (7,6) 是障碍，右邻 (9,6) 为反向/蛇身，上下均可走。
- 原始坐标输入再次返回 left，概率 0.57、confidence 0.44。
- 增强输入保留原始局面，添加四方向的目标坐标、即时碰撞原因、到苹果的曼哈顿距离与是否吃到苹果，并明确先存活再接近食物。返回 down，概率 0.99、confidence 0.99。
- 增强请求第一次发生连接超时，随后同一静态局面再次请求成功；没有将超时计为成功，也没有把探针结果写入对局。
- 完整增强正文和响应位于 .data/qa/context-action-facts-probe.json。此次是单个局面的效果验证，尚未修改正式 runner 的 context，不能据此宣称总体胜率或网络延迟改善。


## 增强 context 的正式单局试跑

用户在固定局面对照后要求启动 500ms 游戏。已将 action-facts-v1 接入正式请求：保留实际棋盘、全部四个选项，附上目标格、即时碰撞原因、苹果距离与吃苹果标识，以及先避免立即死亡再接近食物的说明。代码只提供事实，不覆盖模型返回方向。

类型和存储校验兼容旧字符串选项与新结构化选项；墙外候选坐标允许负值。回归测试把事实与真实 move 规则对照，覆盖反向、边界、障碍、身体、尾格腾出与增长，确认不修改输入状态。33 项测试、类型检查和构建通过后，才启动新对局，避免开发重载中断采样。

真实对局 ID：c52185e2-cc4f-4454-b8f7-c55dd253bffc，Typesafe 直连，500ms/步，独立随机地图。观战页已实际选择该局。

该局最终在 8.5 秒、第 17 步撞墙，0 分；事件中没有模型响应。首个请求在终局时被取消，没有自动重跑，不能用这一局评价增强 context 的方向质量。


## “模型完全没响应”请求链诊断

对上一局 c52185e2-cc4f-4454-b8f7-c55dd253bffc 的相同初始局面和增强 context，绕开游戏时钟直接请求 Typesafe，四次均返回 HTTP 200 与真实方向答案，证明该凭证及正文可用。没有为此新开用户对局。

| 探针 | 建连完成 | 收到首字节/响应头 | 总耗时 |
| --- | --- | --- | --- |
| Node fetch 1 | 509ms | 920ms | 920ms |
| Node fetch 2 | 3076ms | 3497ms | 3497ms |
| curl HTTP/2 | TLS 453ms | 4551ms | 4551ms |
| curl HTTP/1.1 | TLS 700ms | 1000ms | 1001ms |

Node 数据来自 Undici diagnostics_channel，未读取或打印认证头。curl 凭证仅通过 stdin 配置传递，没有放入命令参数或记录文件。DNS 返回两个 IPv4 地址，本组成功连接均为 44.227.31.201；未设置 HTTP_PROXY/HTTPS_PROXY/ALL_PROXY 环境变量。本组观察到建连阶段与连接完成后的等待均会达到秒级，不能据此将整段时间归为纯模型推理，也不能据此断言 H1 永远优于 H2。

报告为 .data/qa/typesafe-network-diagnosis.json，复现脚本为 .data/qa/diagnose-typesafe-network.mjs。上一局没有这些阶段记录，因此只能确认 8.5 秒内未完成调用、终局取消，不能精确还原那一次的连接/服务耗时分布。

额外发现 runner 在终局后会吞掉不属于主动取消的模型异常。新增真实子进程回归用例先复现：终局后的 HTTP 503 原来退出码为 0、错误被隐藏；修复后仅接受与 AbortSignal.reason 相同的预期取消，其余错误明确输出并非零退出。新增请求开始、取消和失败日志，取消/失败使用 elapsedMs，不混入成功响应的 requestMs 统计。已验证 Node 原生 fetch 保留 abort reason 对象身份，且原有慢响应取消测试通过。

34 项测试、前后端类型检查、构建及改动文件 Biome 检查通过。修复范围是错误可见性；外部 API 的秒级建连和响应等待仍存在，没有添加自动重试、改选方向、自动降速或假成功路径。


## 概率总和校验纠正

用户明确指出 0.99 不应阻断游戏。此前将辅助概率的一致性检查升级为致命条件，是客户端实现不当。已改为记录 model_response_warning，返回原始 choice 和 probabilities；页面显示原始合计，不做归一化或代选方向。HTTP 失败、非法方向或无法解析的响应仍会明确报错。

- 单元测试覆盖合计 0.99 与 1.01：方向保持、概率逐项保持、产生诊断而不抛错。
- runner 集成测试持续返回合计 0.99 的响应，经真实 HTTP/WS/SQLite 多步执行后正常撞墙结算，没有 model_error 中断，保存的概率仍为 0.99。
- 36 项测试、前后端类型检查和构建通过。
- 用户已要求切回 OpenRouter，本地 .env 已设置 JEV_PROVIDER=openrouter、JEV_MODEL=typesafe/jev-1.13。更改前启动的旧 runner 对局 ba78e358-d9a3-4e11-9801-6c8f877c2a2b 跑到 234 步、160 分，仍被其已载入的旧校验中断；该历史事实保留，没有改写结果。
- 确认不存在活动局后重启开发服务，健康状态读取新的 OpenRouter 配置。后续启动的 runner 使用修正后的概率处理规则。


## 实时观战入口纠正

历史列表之前对所有状态都链接到 /matches/:id/replay，且首页只有“查看已记录过程”。因此从这些入口进入的是暂停的回放快照，并非 WebSocket 实时页面。已新增 /watch/:matchId，复用实时订阅逻辑，绑定 URL、选中项与订阅目标；ready/running 记录显示“观战”，终局显示“回放”。回放中对未结束记录明确提示不会跟随新进度并提供实时入口。观战页在进行中显示“实时更新”，顶部只激活观战；旧连接清理后的回调不再更新新局面。

真实验收使用本地 HTTP 创建的 c0c29242-7cf2-4eac-9c4d-fbcbfd435876，参与者标注“观战入口验收 · 非 JEV”，1 秒一步，没有模型推理。浏览器从历史的“观战”链接进入绑定地址，初始 ready/tick 0；经真实 WS start 后页面自动到 tick 3。刷新仍在同一 URL、显示 tick 3。点击回看片段进入 replay/tick 0 且显示模式提示；返回实时页面后自动到 tick 5。验收局最终正常在 tick 17 撞墙。

用户原对局 3e98b0a9-df25-4731-85b8-d3221fcbd267 已在 tick 438、290 分时撞墙。观战页面最后状态与数据库一致，终局停止运动属于预期行为。浏览器另验证切换对局参数、导航激活、375px 布局与无页面错误；类型检查、构建、Biome 和 diff 检查通过。
