## 1. 公共契约与持久存储

- [x] 1.1 在 shared/snake/community.ts 定义反馈、企微配置、贡献/撤回、验证回执和管理命令的严格契约；新增 community-schema 测试验证空白正文、未知服务商、缺失授权、冲突字段和公开 DTO 禁止秘密字段。
- [x] 1.2 在 server/db/store.ts 接入 schema v3 迁移，新增反馈、幂等回执、验证、贡献凭证、私有调用及轮换绑定存储；用临时真实 SQLite 验证空库/v1/v2 升级、二次启动和事务失败，逐字比较旧 matches/events/watch 数据不变。
- [x] 1.3 新增 server/credentials 的加解密与 HMAC 去重实现，解析独立主密钥；验证随机 nonce、AAD/密文篡改、错误主密钥、同 Key 去重、掩码，以及数据库中无测试用原始秘密。

## 2. 反馈与企微服务

- [x] 2.1 实现反馈投稿、稳定游标分页及可见性存储；HTTP/SQLite 测试覆盖即时公开、同请求重发仅一条、冲突、写失败、隐藏后公开查询不返回正文。
- [x] 2.2 装配 /api/community 与公共写入 Origin、流式请求体大小校验；测试链接/二维码均有、缺失、非法协议、413、大小限制显式关闭，以及投稿不能取得游戏写权限。
- [x] 2.3 在 /api/watch-admin/* 下接入反馈管理并复用同一个 OwnerSessions；测试有效/过期/伪造会话、错误 Origin、幂等隐藏与恢复，不改写反馈原文。

## 3. 贡献验证与生命周期

- [x] 3.1 为 server/jev/client.ts 增加结构化安全错误，按 design.md 已核对契约区分 Typesafe 401、OpenRouter 明确凭证/余额错误、临时预算和其他失败；用两个服务商的响应 fixture 验证分类，未知错误不得转为可轮换，现有概率/原文测试继续通过。
- [x] 3.2 实现独立固定请求的真实验证服务，复用现有 sendJevRequest；契约测试覆盖合法结果、HTTP 200 错误正文、模型别名、未授权、权限/限流/网络失败，并断言验证不创建对局、不修改游戏或 RNG。
- [x] 3.3 实现贡献 POST 与状态查询、验证前登记和成功后原子加密入池；通过并发请求及故障注入测试验证同请求不重复调用、同 Key 合并、同标识不同内容冲突，以及验证成功但存储失败不启用。
- [x] 3.4 实现验证期间关闭服务、遗留 validating 转 unconfirmed 和显式重试；重启测试证明不重放外部请求、不假报成功，回执可明确区分已启用和结果未确认。
- [x] 3.5 实现持有原 Key 的撤回与管理员 disable / revalidate-and-enable；测试撤回不调用外部服务、密文清除、重复撤回幂等、管理员不能复活 revoked、禁用与迟到验证竞态不重新启用。
- [x] 3.6 实现私有凭证管理列表、调用/验证统计与错误摘要；测试已验证未使用、服务商/模型不匹配、缺失 usage/费用不补零，HTTP/WS、日志、异常 cause 与错误正文无秘密泄露。

## 4. 连续观战实际使用凭证池

- [x] 4.1 实现同 provider/目标模型候选、环境 Key 与贡献去重、逐局稳定轮换和池开关；测试 A/B 正常轮换、不同服务商隔离、验证不推进游标、禁用/撤回跳过、模型配置变化要求重新验证。
- [x] 4.2 在 server/watch/channel.ts 的启动/开局/恢复接入凭证源，原子保存新局和凭证绑定；验证无环境 Key 但有贡献可开局、创建失败不消耗游标、health 反映实际可用性且不暴露池成员。
- [x] 4.3 给 runJevMatch 增加可选凭证源和私有 attempt 生命周期，在发出前检查状态并登记、响应后保存真实结果，关联独立 action_request_id；测试仍为单在途，CLI/关闭池模式保持原错误退出，不因回执丢失再次调用模型。
- [x] 4.4 实现明确凭证错误的事务排除与串行切换；HTTP/WS 测试验证 A 失效→B 有效只推进一步、失败事实可查、切换前重读过期奖励/观察，429、权限、网络、未知错误和临时预算一律暂停。
- [x] 4.5 实现池耗尽故障与无可用凭证状态；测试全部失效不循环调用、无另一服务商兜底，新贡献不自行清除 fault 或唤醒 stopped，管理员明确恢复后可使用新贡献。
- [x] 4.6 将凭证选择接入同局恢复、draining、stopCurrent 和 generation 取消逻辑；运行 watch-resume-api、watch-stop-current-api、watch-channel 相关测试，新增“在途撤回只允许已发调用结束”“立即停止先提交后无新增请求”“重启保留身体/RNG/时间/游标”竞态覆盖。
- [x] 4.7 注入私有调用/池状态存储失败及解密失败；验证不提交未记录模型结果、不继续消耗其他 Key、不伪装池为空，既有历史和停滞费用保护回归通过。

## 5. 页面与管理交互

- [x] 5.1 新增 /feedback 路由及表单/列表并生成路由树，扩展 Header 的反馈、企微与贡献入口；验证直接刷新、高亮、即时投稿、隐藏后读取、空/错状态及 HTML 文本不执行。
- [x] 5.2 实现企微弹窗和配置读取；验证真实链接/二维码、未配置、加载失败与移动端提示，打开外部入口不虚报入群成功。
- [x] 5.3 实现贡献弹窗的服务商选择、密码输入、主动授权、验证中/已入池/未确认/失败状态与撤回入口；验证重复点击不重复请求、关闭清除 Key、重新打开可查本次回执、文案说明费用与当前服务商调度关系。
- [x] 5.4 在 /watch?admin=1 已解锁区增加反馈和凭证管理面板；验证普通页面隐藏、过期返回解锁、反馈隐藏/恢复、Key 禁用/重新验证以及真实使用记录显示。
- [x] 5.5 按现有风格完成必要素材与弹窗动效：需要插画、线稿或动效配套图片时使用用户已授权的 image_gen，并保留提示词、选用素材及预览；不需要则记录复用现有素材的决定。验证素材不含密钥或伪造二维码，减少动态效果下操作与信息完整。
- [x] 5.6 实现贡献验证且入库成功后的感谢状态与一次局部庆祝动效，参考 transitions-dev 的成功反馈；验证感谢文案/已入池确认持续可见、约 650ms 动效后静止、失败/未确认不触发、重复回执不重播、后台完成不强行重开，以及减少动态效果与读屏提示正确。

## 6. 集成验收与部署资料

- [x] 6.1 运行新增 community/credential 测试及 tests/jev.test.ts、tests/jev-runner.test.ts、tests/watch-store.test.ts、tests/watch-api.test.ts、tests/watch-resume-api.test.ts、tests/watch-stop-current-api.test.ts、tests/watch-stagnation-api.test.ts；记录实际结果，使用 bun run test 而非 bun test，测试子进程不读取开发密钥。
- [x] 6.2 执行 bun run typecheck、bun run build 及任务文件的格式/lint 检查，确认前后端与生成路由均成功；记录失败或未验证范围，不把 File ignored 视为格式验证通过。
- [x] 6.3 用真实浏览器验证桌面/375px、Tab/Escape/焦点返回、管理员隐藏反馈、贡献全部状态、成功感谢动效的一次播放和减少动态效果下静态感谢。开始前读 agent-browser --help，使用唯一 session 并记录 daemon/浏览器 PID；结束 close 并核对 session list 与完整命令行无本任务残留，保存截图与清理结果。
- [ ] 6.4 在具备明确授权且通过安全服务端配置提供的验收 Key 时，对 Typesafe/OpenRouter 各完成一次真实验证，并在隔离验收库中证明贡献 Key 实际参与受控连续观战。保存脱敏请求、响应、时长及实际调用记录；不在聊天、命令参数或提交产物中放 Key。缺少某服务商凭证时明确记录该项真实验收未完成，不用传输替身或 HTTP 200 代替。
- [x] 6.5 针对当前 OpenRouter alpha/decisions 端点留存可取得的脱敏凭证错误样本与分类证据，核对普通余额耗尽和临时预算阻塞不会混淆；Typesafe 未确认的额度错误仍按暂停处理，不通过主动耗尽账户制造样本、不自行扩大可轮换错误类型。
- [x] 6.6 更新 .env.example、README.md 的新公共写入边界、企微配置、生产 HTTPS、加密秘密、两个启用开关、轮换/暂停/撤回语义及 v3 升级回滚步骤；核对命令和真实配置，保留已有 .env，提供不含秘密的验收记录。
