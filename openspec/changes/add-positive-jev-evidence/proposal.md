# JEV 自主玩家的正向证据

用户确认 JEV 每一步自主选择四个方向，程序只提供事实。当前死亡证明能够识别部分陷阱，但静态摘要遗漏动态绕行机会，且找到的路线被压缩成距离或布尔值。

本变更增加当前苹果路线、无增长循环、身体释放时序，以及与先前见证相容的当前几何。新单步请求使用 action-facts-v4，旧上下文保持原文可读。完整路线独立存档，不过滤动作、不评分排序、不执行路线、不替换模型选择。

生产沿用当前 response/single_step。两步 builder 仅保留离线历史兼容，不重新开放已停用模式。
