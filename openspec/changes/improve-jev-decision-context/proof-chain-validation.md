# 多层死亡证据与模型输入优化验收

日期：2026-09-19。保留“游戏规则与事实由代码提供，策略由 JEV 决定”的边界，不加入方向排序、过滤、代选、重试或循环终止。

## 实测问题

- 大图对局 `334e1e69-17b0-4875-8f48-916a8d4b1db8` 在1633步撞障碍，1080分、84个苹果、长度88。最后一步已经所有方向都碰撞或被证明必死，不能单独归因于最后一次选择。
- 关键可避免入口为 tick1628 / seq3274，蛇头(5,9)，选择left。原输入中 up/left 的关键摘要相同：danger=null、15静态格、bodyLength=88、尾不连、branch1、无静态苹果路径。
- 独立用真实引擎枚举left后的136个合法状态，最多13次成功移动，第14步内所有分支终止。up有15步逃到(0,9)的见证，此时306静态格、尾连；完整53步可吃到当前苹果。

## 实现

- 复用已有单分叉证书作为可信叶子；若当前分叉只剩一条未证路线，则继续检查该路线，保留所有其他合法分支的死亡证书，并在终点完成合取。
- 使用迭代frame折叠，真实唯一合法通道才计入forcedPrefixMoves；一个未证方向不等于其他方向非法。
- 多个未证分支、完整身体与朝向循环、未证明的苹果后续仍返回未知，获胜优先；没有深度、时间或状态数预算。该方法不穷尽任意复杂分叉。
- 新输入 left=proven_fatal，进入后保守上界15步（真实最迟14步，因此是有效宽松上界）；up仍为未证明危险。
- 模型只接收入口/首分叉上界、证明种类和证明结构计数。完整树保留在分析器与测试中；减少嵌套JSON在模型指令中的重复，不改变任何证据的结论或动作集合。

## 真实 JEV 调用

| 输入 | 选择 | up概率 | left概率 |
| --- | --- | ---: | ---: |
| recorded-input | left | 43% | 51% |
| chain-evidence-1 | up | 90% | 5% |
| chain-evidence-2 | up | 94% | 3% |
| chain-evidence-3 | up | 90% | 6% |

预先安排旧输入1次、新输入3次。保持当前 OpenRouter / typesafe/jev-1.13，未更换模型，未通过重试挑选结果。新三次均自主向上；它说明该固定局面的输入改进有效，不证明任意地图必胜。

## 成本与验证

- 全量20个文件 / 217项测试通过；类型检查、oxfmt/oxlint与生产构建通过。
- 旧完整通关记录逐步重算：653个实际成功动作，误报必死0个；最新逃生见证的53个动作也没有被误报。
- 该局面完整证书2661字节，模型侧摘要276字节。
- 新请求总量5744字节，旧保存请求4685字节：新增事实仍有输入成本，不能宣称整个请求变小。
- 本机100次构建p50=1.882ms、p95=2.244ms、最大3.944ms；只作局部成本观察。

## 证据与范围

- `tests/fixtures/multilevel-branch-trap.json`：真实局面、旧输入、136状态统计和可行逃生路线。
- `.data/qa/autonomy-optimize/failure-audit.json`、`probe.json`、`local-cost.json`、`winning-route-check.json`。
- `.data/qa/autonomy-optimize/{tests,typecheck,build}.log`。
- 上述固定局面实验未启动新对局；后续真实分支续跑见下一节。

## 从原卡点真实续跑

- 新分支 `2b49c199-ee8c-4d63-9125-a78b4d4d19f9` 从原局 seq3273 / tick1628 继续，沿用 24×18、12 障碍、response/single_step、OpenRouter/typesafe/jev-1.13。
- 恢复时1080分、长度88、gameTimeMs=595989.0477919998。逐事件重放恢复RNG，并校验所有物理字段。继承3274条事件，除分支ID和来源外逐条一致；原局matches/events/control_requests内容指纹前后相同。
- 首个真实模型请求观察tick1628、目标tick1629，保留37步未吃苹果的历史；JEV自主选择up，概率89%，该动作真实执行。没有补演之前的模型调用。
- 此后选择left、left、left、up；总共5次真实决策，第1633步撞障碍结束，仍1080分。只跑此一个分支，没有重试挑选成功结果。
- 新的关键入口是观察tick1630、蛇头(4,8)时的left。此时down/left均未标记必死；left后仅57个合法状态，最多再成功移动10次、随后无合法续行。down有13步到(0,9)并共51步吃到苹果的真实引擎见证。因此本次结果只验证原卡点第一步得到改善，未解决所有更深分叉的证据缺口。
- 漏证发生在同一入口出现两条需要更深证明的出口时：当前链式分析遇到两条未知出口即返回null；进入该分叉后，分别分析各出口又能判死。此次续跑没有修改分析算法或代替模型选方向。
- 回放默认定位续跑起点，保留从头回放和原局链接。浏览器验证起点1628、从头0、恢复起点1628、下一步1629及模型up=89%，页面无运行错误。
- 分支功能与回归共24文件 / 246项测试通过，类型检查、改动文件格式/lint及生产构建通过。一次全套出现固定步频测试额外fallback计数，原执行范围复跑未复现；测试现明确在目标恢复plan接纳后停止，保留唯一fallback断言，并验证模型观察序列为[0,1,2]。
- 证据：`.data/qa/fork-1628/{source-before.json,result.json,events.json,runner.log}`。继承的原始上下文保持原样，新决策使用当前上下文。
