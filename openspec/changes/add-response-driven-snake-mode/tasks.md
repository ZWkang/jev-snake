## 1. 模式契约与兼容读取

- [x] 1.1 在共享类型及校验中增加 fixed/response，明确 response 的空固定间隔和无截止元数据；测试默认 fixed、合法 response、未知模式及显式冲突参数。
- [x] 1.2 增加新响应记录版本及前后端双版本读取；以真实旧库夹具验证 v1 状态、事件、创建请求重发和摘要不被默认字段改变，未知版本明确失败。

## 2. 服务端推进与持久化

- [x] 2.1 分离移动调度与奖励到期处理，response 不安排移动定时器；用可控时钟验证等待 10 秒仍为 0 步，而 fixed 仍按原间隔推进。
- [x] 2.2 实现响应动作校验后立即移动的一次事务；测试 120/470/1770ms 的真实间隔、重复请求、同观察竞争、非法反向、碰撞及写入失败回滚。
- [x] 2.3 提供真实运行时间与每步间隔，response 的 nextTickAt/deadlineInMs 为 null；测试星星等待中到期、到期优先于同时动作、旧摘要拒绝且不移动。
- [x] 2.4 保留停止、控制断线和重启语义；验证等待中停止保存真实时长、断线不自动走、重启只保留已确认位置而不补步。

## 3. Runner 与模型输入

- [x] 3.1 增加 --step-mode 与对应配置说明，创建前拒绝显式冲突；测试 response 不继承固定步长环境值，默认启动仍为 fixed，不留下无效配置的对局。
- [x] 3.2 实现 response 的请求—提交—确认串行循环；用快慢传输测试确认最多一个在途请求、有效结果立即走一步、没有额外 sleep，拒绝后可重新询问而不死等 tick。
- [x] 3.3 更新真实请求中的模式与时间字段并保留原始正文；测试没有伪造固定截止，API 错误、主动取消及概率合计诊断仍按已有规则暴露。

## 4. 观战、历史与回放

- [x] 4.1 在各页面展示推进模式、响应等待、实际间隔及平均步频；验证零步、同时间戳、长等待、断线和终局状态，不出现 Infinity/NaN 或虚构配置速度。
- [x] 4.2 让 response 棋盘直接跟随已确认状态，将得分反馈与固定间隔插值解耦；浏览器验证快速连续响应、奖励变化、375px 布局和减少动态效果。
- [x] 4.3 按原事件时间回放变步频记录；测试非等间隔、同时间戳 seq 顺序、倍速、逐步与旧 v1 回放一致性，不重新调用模型。

## 5. 集成验收与交付

- [x] 5.1 用真实 SQLite 与 HTTP/WS 完成两种模式并存、观众重连、请求去重和游客权限验收，确认记录连续且写入前无成功广播。
- [x] 5.2 运行相关规则/runner/回放测试、类型检查、构建和改动文件 Biome；使用真实模型分别验证 response 与 fixed，记录实际返回、移动时刻与失败，不以模拟结果替代。
- [x] 5.3 更新 README、环境示例及验收记录，给出两种启动命令、时间含义、旧记录兼容与两步草案边界；逐项核对文档和实际接口一致。
