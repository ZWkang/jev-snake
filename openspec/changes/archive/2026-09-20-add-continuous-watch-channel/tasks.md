## 1. 频道契约与存储

- [x] 1.1 定义频道快照、阶段、命令、回执和错误契约，明确 revision 与单局 seq 的区别；用 schema 测试验证各阶段必需字段、非法输入及公开对象不含凭证。
- [x] 1.2 实现 SQLite v1→v2 迁移和新库初始化，新增频道、归属轮次与命令去重表；用含旧单步/两步/response 记录的临时库验证升级后原 JSON、历史和回放不变，重复打开不重复迁移，迁移失败不换空库。
- [x] 1.3 实现新局创建、created 事件与频道归属的同事务登记，以及频道快照/命令回执持久写入；通过写入失败注入验证没有孤立半创建局、部分确认或提前广播。
- [x] 1.4 实现显式 enabled 命令的持久去重；测试同请求重发、同 ID 不同内容、重复开启/停止，以及“开启 A→停止 B→重发 A”仍停止。

## 2. 复用真实单局运行器

- [x] 2.1 将单局控制循环提取为无顶层执行副作用的模块，接受已创建局及专属凭证、明确配置和取消信号，返回真实结果/错误；通过现有 HTTP/WS/SQLite runner 回归验证旧观察绑定、单在途、progress 事实和错误未改变。
- [x] 2.2 将 CLI 改为创建单局后调用共享模块，保留参数、默认值、冲突校验、日志与退出码；验证 fixed 单步、fixed 两步、response 单步及服务商配置隔离用例通过，CLI 仍只打一局。
- [x] 2.3 抽取频道与 CLI 共用的配置解析，增加默认 5 秒局间时间且不改变局内节奏；测试环境优先级、无 seed 时新种子、固定 seed 保留、缺少模型密钥和无效模式在开局前显式失败。

## 3. 连续频道生命周期

- [x] 3.1 新增由游戏服务持有的频道调度器，初始停止，手动开启后准备第一局，正常终局且运行器清理成功后进入倒计时并续局；用可控时钟验证完整两局流程及无观众时照常运行。
- [x] 3.2 实现运行中的停止续局与重新开启：不取消本局模型请求、不发送单局 stop；测试 running→draining→stopped 及 draining→running，验证重新开启沿用当前局。
- [x] 3.3 实现倒计时/starting 阶段停止和 start 提交时的频道校验；测试停止先提交、start 先提交、旧定时回调、重复终局通知和旧 generation 回调，不产生停止确认后的新 start 或并行所属局。
- [x] 3.4 实现故障状态、公开错误脱敏和所有者明确恢复；测试网络/API/格式错误、终局相邻真实错误、外部异常中断及持久化失败，验证不自动换模型、重试或新建替代局，未落库错误不报告已保存。
- [x] 3.5 接入服务监听成功后的恢复流程，清理频道所属遗留 ready 局，保留旧局中断记录；测试 stopped、draining、running、countdown、starting、fault 各阶段重启，手动 ready 局不被接管，已保存故障不自动清除。
- [x] 3.6 接入进程关闭顺序并按对局模式选择正确控制协议；测试关闭与倒计时同时发生、单步/两步在途请求清理、持久 enabled 不被进程退出改写，关闭后无残留计时器或模型请求。

## 4. 公开频道同步与所有者权限

- [x] 4.1 增加公开频道 GET 与只读 WS 完整快照推送，先订阅后取快照并按 revision 合并更新；用两个真实 WS 客户端验证订阅期间换局不丢失、重连获得最新局、游客控制消息被拒绝。
- [x] 4.2 实现基于现有 GAME_ADMIN_TOKEN 的口令解锁及独立 HttpOnly 会话、查询和注销接口；测试正确/错误口令、伪造/过期会话、注销及重启失效，公开响应/日志/构建不含秘密。
- [x] 4.3 配置管理员会话有效期和可信前端 Origin，按本地 HTTP / 部署 HTTPS 设置 cookie 属性；测试跨站和缺少/不符 Origin 的写请求被拒绝，正常同源代理访问能够解锁及控制。
- [x] 4.4 实现所有者启停 API 与持久回执，所有请求在服务端鉴权，过期会话返回 401；真实 HTTP 集成验证只加 admin 参数不能操作，合法会话可以启停且重复命令不重复开局。

## 5. 首页、频道与固定单局页面

- [x] 5.1 将现有介绍、素材、规则和最近历史提取为稳定首页，增加并列的“进入连续观战 / 翻看历史对局”按钮；浏览器验证无局和有活动局时 `/` 都保留首页，点击仅导航。
- [x] 5.2 新增 `/watch` 索引路由及 admin 搜索参数校验，更新顶栏“首页 / 观战 / 历史对局”和路由生成文件；运行路由生成/类型检查，并验证三个导航高亮、动态单局链接和历史回放兼容。
- [x] 5.3 提取可复用单局观测与展示，频道页面只跟随频道 matchId；用浏览器/受控事件验证自动跨局、清理旧订阅与迟到回调、固定单局不跳转、手动 CLI 新局不抢占频道。
- [x] 5.4 实现结果保留、服务端时间倒计时、准备中、draining、停止及故障显示；验证倒计时归零不自行开局、不出现负数，后台恢复和断线重连校正状态，上一局回放链接正确。
- [x] 5.5 在且仅在 `/watch?admin=1` 显示解锁入口及通过鉴权的控制区，支持显式开启、停止续局、注销和错误提示；验证参数移除隐藏控件、会话过期回到解锁、注销/关闭页面不改变频道开关。
- [x] 5.6 保持整页自适应与现有视觉，首页双按钮小屏换行、棋盘等比缩放、侧栏下移，倒计时与控件可键盘访问；按 AGENTS.md 用独立 agent-browser session 检查 1440×1000 与 375px 窄屏截图，无横向溢出，减少动态效果仍可读。

## 6. 联合验收与交付记录

- [x] 6.1 补充环境示例和 README：连续频道/CLI 区别、管理参数只控制显示、口令来源、Origin/会话/倒计时配置、重启意图、故障恢复及数据库迁移回退限制；按实际接口与启动命令逐条核对，不写未实现能力。
- [x] 6.2 运行完整测试、`npm run typecheck`、`npm run check` 和 `npm run build`；记录命令结果及任何失败，确认不把测试替身结果写成真实模型游玩。
- [x] 6.3 使用真实服务端和两个只读浏览器页，加一个已解锁管理员页，验证多观众同局、停止与换局竞态、刷新、关闭所有观众页、断线跨局、权限拒绝及固定单局/回放不被自动切走；在本 change 的 validation.md 保存结果和截图路径。
- [x] 6.4 在后续实施授权下使用已配置真实 JEV 完成至少一次自然跨局，再于第二局进行中提交“本局结束后停止”；记录真实 matchId、频道 revision、自然终局、下一局开始及停止后无新局证据；若尚未自然结束则保持此项未完成，不添加限步或强制终局凑结果。
